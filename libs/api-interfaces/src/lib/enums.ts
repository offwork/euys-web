export enum AktifPasif {
  AKTIF = 'A',
  PASIF = 'P',
}

export enum EvetHayir {
  EVET = 'E',
  HAYIR = 'H',
}

export enum IslemTipi {
  KAYIT = 1,
  GUNCELLEME,
  SILME,
}

export enum YerliIhrac {
  'YERLİ' = 'Y',
  YERLI = 'Y',
  'İHRAÇ' = 'I',
  IHRAC = 'I',
}
