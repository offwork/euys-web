import { ImagePreviewOverlayRef } from './image-preview-overlay-ref';

describe('ImagePreviewOverlayRef', () => {
  it('should create an instance', () => {
    expect(new ImagePreviewOverlayRef({} as any)).toBeTruthy();
  });
});
