import { TableMultiSelectItemDirective } from './table-multi-select-item.directive';

describe('TableMultiSelectItemDirective', () => {
  it('should create an instance', () => {
    const directive = new TableMultiSelectItemDirective();
    expect(directive).toBeTruthy();
  });
});
