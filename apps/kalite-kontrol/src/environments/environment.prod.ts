import { AppEnvironment } from '@euys/core';

export const environment: AppEnvironment = {
  devTools: true,
  devs: false,
  e2e: false,
  local: false,
  localJsonServer: true,
  production: false,
  test: false,
};
