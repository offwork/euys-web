import { AppEnvironment } from '@euys/core';

export const environment: AppEnvironment = {
  production: true,
  e2e: true,
  devs: false,
  local: false,
  localJsonServer: false,
  devTools: false,
  test: false,
};
